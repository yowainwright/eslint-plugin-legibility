# eslint-plugin-legibility Codex Rules

Use `.agents/skills/eslint-plugin-legibility/SKILL.md` as the shared agent skill workflow.

Generate the shared skill with `nub run build:agent -- --target agents` when it is missing.

Keep this file generated from `scripts/agent/constants.ts`.